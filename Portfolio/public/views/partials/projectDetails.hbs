<div class="projectDetailsPage">
    <div class="slideshow_container">

        {{!-- Big pictures --}}
        {{#each projectData.popup_pics as |pic pic_index|}}
        <div class="big_slide" id="big_slide{{pic_index}}">
            <img src="{{pic.src}}" alt="{{pic.alt}}">
        </div>
        {{/each}}


        {{!-- Small pictures --}}
        <div class="small_slide_container">
            {{#each projectData.popup_pics as |pic pic_index|}}
            <div class="small_slide" id="small_slide{{pic_index}}">
                <img src="{{pic.src}}" onclick="showSlide({{pic_index}})" alt="{{pic.alt}}">
            </div>
            {{/each}}
        </div>

    </div>

    <div class="information_container">
        <h1>{{{projectData.title}}}</h1>
        <h2 class="project_date">{{projectData.startDate}} - {{projectData.endDate}}</h2>
        <p>{{projectData.information}}</p>
        <h2>Link: <a class="link" href="{{link}}" target="_blank">{{projectData.link}}</a></h2>
    </div>
</div>